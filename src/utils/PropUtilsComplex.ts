import type { ComplexValueType } from "../types-extra"

export default class PropUtilsComplex {
	public static check(complex: ComplexValueType) {
		switch (complex) {
			case "Array3Nums":
				break
			default:
				break
		}
	}

	public static checkArray3Nums() {}
}
